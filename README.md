# Phone Camera App 📱

## Run These Commands:

```bash
npm run dev
ifconfig | grep "inet " | grep -v 127.0.0.1
```

## Access the App:

- From your computer: `http://localhost:5001`
- From your phone: `http://YOUR_IP:5001` (replace YOUR_IP with the IP from the command above)

Tap "Advanced" → "Proceed" when you see the security warning (if using HTTPS)
