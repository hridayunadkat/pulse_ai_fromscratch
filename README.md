# Phone Camera App 📱

## Run These Commands:

```bash
npm run dev
ifconfig | grep "inet " | grep -v 127.0.0.1
```

## On Your iPhone Safari:

Open: `https://YOUR_IP:3000` (replace YOUR_IP with the IP from the command above)

Tap "Advanced" → "Proceed" when you see the security warning, then tap "Start Camera"
